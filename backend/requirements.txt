fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
python-dotenv>=1.0
httpx>=0.27

# LLM sağlayıcıları (sağlayıcı-bağımsız katman üzerinden kullanılır)
anthropic>=0.40
google-genai>=1.0

# Veri katmanı (Supabase REST + pgvector RAG)
supabase>=2.0
